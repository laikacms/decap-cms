// Canonical re-export of the Emotion styling helpers shared by `src/ui/`
// primitives (the `variants` cva-style helper, `css`/`cx`/`keyframes`, and
// the `WithClassName` type). Physical implementation still lives at
// `src/lib/widgets/editor/ui/_styled.ts`. See ./README.md.
export * from '@/lib/widgets/editor/ui/_styled';

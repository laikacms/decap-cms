import * as stringTemplate from './stringTemplate';
import * as validations from './validations';
import * as bidiControls from './bidiControls';
import { getFieldAriaProps } from './fieldAria';

export const DecapCmsLibWidgets = {
  stringTemplate,
  validations,
  bidiControls,
  getFieldAriaProps,
};
export { stringTemplate, validations, bidiControls, getFieldAriaProps };
export type { FieldAriaProps } from './fieldAria';
